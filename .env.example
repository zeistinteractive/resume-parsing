# Copy this file to backend/.env and fill in your values

# Gemini API key for AI resume parsing
GEMINI_API_KEY=your_gemini_api_key_here

# DATABASE_URL is set automatically by docker-compose.
# Only needed when running the backend outside Docker:
# DATABASE_URL=postgresql://resume_user:resume_pass@localhost:5432/resume_engine
